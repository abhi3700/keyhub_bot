- solve `db_postgres.py` to store the CSV to SQL
- Today
  - check for phone no. reading
  - check for the database storing in redis DB & excel DB

- check if a user from a channel using `bot.py`
 